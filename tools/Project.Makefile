# Legacy shim — content has moved to tools/drivers/project.mk.
# Kept as the public entry point for a top-of-tree multi-subdir Makefile.

BUILD_ROOT	?= $(THORSANVIL_ROOT)/build
include $(BUILD_ROOT)/tools/drivers/project.mk
